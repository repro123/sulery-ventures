---
import type { NavLink } from "@/lib/types";

interface Props {
  link: NavLink;
  className?: string;
}

const { link, className = "" } = Astro.props;
---

<li class={className}>
  <a href={link.href}>{link.label}</a>
</li>
